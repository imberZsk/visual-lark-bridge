# 安全策略

## 支持版本

项目处于早期开源阶段，仅维护 `main` 分支上的最新版本。

## 报告漏洞

请通过 GitHub Security Advisory 私下报告安全问题，不要在公开 Issue 中提交 App Secret、访问令牌、飞书消息内容、Claude 会话日志或可复现的敏感数据。

报告时请包含受影响版本、复现步骤、潜在影响和建议修复方向，并对凭证与个人数据做脱敏处理。

## 安全边界

本项目连接飞书机器人和本机 AI 编码 Agent，当前 provider 为 Claude Code，可能按用户指令读取或修改工作区内的文件。使用者应当：

- 为机器人创建独立飞书应用并遵循最小权限原则。
- 通过 `lark-cli profile add --app-secret-stdin` 把 App Secret 保存到系统安全存储。
- 不提交 `.env`、日志、附件、AI 会话和自定义工作区说明。
- 把 Agent 工作区限制在明确授权的目录，不要默认指向整个用户主目录。
- 仅从可信来源安装代码，并在升级前审阅安装脚本。

# Changelog

## 0.2.1 - 2026-08-09

- 统一桌面端 UI 规范与 4px 间距体系，将服务、设置和日志样式拆分到组件目录，改用 Ant Design 语义色，并新增关键页面无头 E2E 截图验收。
- 桌面端重复启动时可识别并接管同一运行目录中已存在的桥接进程，不再把正常的单实例锁保护显示为 `code=1` 启动异常，并支持继续停止或重启该进程。

## 0.2.0 - 2026-07-28

- 新增 AI 新闻定时推送：桌面端可通过时间选择器配置 RSS/Atom 信息源、每日时刻、目标飞书会话和单次条数；桥接服务自动去重、调用 Claude Code 或 Codex 摘要并主动发送 Markdown 消息。
- AI 新闻通知支持在飞书会话与飞书自定义机器人 Webhook 之间选择，Webhook 地址仅保存在本机且不会写入运行日志。
- 优化日志查看：默认只展示最近日志，支持按系统或任务分类、全文查询、按需加载更早记录，并可解析和复制日志中的 JSON 数据。
- 修复日志查询框溢出、新闻定时任务离线错过后不补发及失败不重试的问题；同一任务的卡片续问改为串行处理，并记录输入框清空结果。

## 0.1.1 - 2026-07-26

- 修复飞书卡片连续对话时，动态任务元数据破坏正文公共前缀，导致已完成的历史回答被重新逐字播放的问题。

## 0.1.0 - 2026-07-23

- 固定 CI 与 Release 使用的 Ruff 版本，避免未锁定的代码检查工具升级后因新增规则导致已验证代码突然无法发布。
- 优化任务命名和任务管理体验：寒暄消息使用“新对话”占位名并在首次实质续问时自动升级；飞书任务中心改为每页 5 项的紧凑分页列表，低频管理操作按任务展开；桌面端任务列表收进右侧抽屉，避免任务增多后服务页无限变长。
- 修复桌面端重复启动桥接进程后，新旧进程内任务状态不一致导致卡片第二次按 Enter 无法发送的问题；同一运行目录现在只允许一个监听实例，并自动淘汰已关闭流式模式的旧卡片映射。
- 修复应用启动时主题与配置并发写入争用同一个临时文件导致 `config.json.tmp` 重命名失败的问题。
- 修复飞书卡片流式输出过程中因心跳和定稿切换正文结构而出现的内容回退、重新流式播放问题。
- 参考 Visual Worktree 将 Toast 全局居中，并将所有 Ant Design 弹层的动画原点固定在屏幕中心。
- 修复任务删除和日志清空 IPC 未完整暴露的问题；统一确认弹层使用全局 Ant Design 上下文、居中动画和明暗主题，统一 Toast 的时长与数量配置。
- 优化日志确认弹层为居中展开并跟随明暗主题，增加刷新与清空按钮间距。
- 日志页新增带二次确认的清空日志操作，清空后立即同步页面内容。
- 任务列表新增删除操作，使用二次确认并要求桥接服务停止后执行，避免运行中的任务状态被重新写回。
- 服务页新增任务状态列表，展示任务 ID、名称、执行状态、轮次和最近问题，并每 2 秒自动刷新。
- 收敛飞书回答卡片操作，只保留任务列表、停止和新任务等核心按钮，并统一任务列表按钮尺寸。
- 修复卡片发送后输入框残留已发送文本的问题，替换输入组件时显式写入空默认值。
- 设置页新增 Claude Code/Codex CLI 提供方切换及 Codex 模型配置；启动失败时保留 Python 进程的具体错误，便于定位 Profile 或 CLI 配置问题。
- 修复 macOS Python 3.14 未加载系统 CA 导致飞书 WebSocket 事件网关握手超时的问题。
- 将主题偏好写入 Electron 用户数据目录的 `config.json`，并保留 localStorage 作为首屏缓存，确保主题在重启后稳定恢复。
- 启用“登录时启动桥接”后，应用无论由系统登录还是用户手动打开，都会自动启动桥接服务。
- 桌面端按独立进程组管理桥接主进程与事件网关，停止、重启或退出应用时不再遗留孤立网关。
- 修复桥接启动竞态：主进程退出后继续清理刚生成的事件网关进程。
- 为 macOS 隐藏标题栏补充独立顶部拖拽区域，窗口可通过顶部留白移动。
- 将项目、应用、数据目录、服务标识、Bundle ID、sidecar 和安装包统一重命名为 Visual Lark Bridge / `visual-lark-bridge`，不迁移旧产品数据。
- 使用新的紫色应用 Logo，并为 macOS 菜单栏提供透明单色桥接图标。
- 参考 Visual Worktree 的 Ant Design 默认主题体系，重新调整亮色、暗色与交互状态配色。
- macOS 安装包使用免费 ad-hoc 完整性签名，不依赖付费开发者证书。
- 新增 Electron 桌面控制台，支持桥接服务、登录自启、配置、诊断、日志、旧服务迁移和运行数据清理。
- 新增可持久化的黑白模式，并将控制台调整为更紧凑的 Ant Design 组件布局；服务状态与本机诊断统一为自适应宽度的单一工作区。
- 将 Python 桥接主进程与飞书官方 SDK WebSocket 网关打包为两个随应用分发的 sidecar。
- 统一产品、运行目录、服务与默认 profile 名称为 Visual Lark Bridge / `visual-lark-bridge`。

# 飞书 Claude 助手功能说明

飞书 Claude 助手把飞书消息实时交给本机 Claude Code 处理。你可以在飞书中连续提问、查看执行进度、管理多个任务，也可以让 Claude 分析图片、文件、语音和引用消息。

## 一、开始使用

直接给机器人发送一条普通消息即可开始。每条普通飞书消息都会创建一个新任务和一张新流式卡片；需要保留上下文继续提问时，请使用对应卡片内的输入框。

例如：

```text
帮我检查这个项目最近的报错，并给出修复建议
```

如果需要把不同工作分开，可以使用 `/new` 创建多个独立任务。

## 二、实时回答与进度

收到问题后，机器人会立即显示进度卡片，而不是等待完整答案生成后才回复。同一个任务会持续复用同一张卡片，减少重复消息。

卡片可能显示以下阶段：

- **思考中**：Claude 正在理解问题或规划处理步骤。
- **搜索中**：正在搜索网页、代码或工作区内容。
- **读取文件**：正在读取本机文件。
- **执行命令**：正在运行命令或修改文件。
- **整理答案**：工作已经基本完成，正在组织最终回复。

卡片还会显示任务名称、本轮耗时、当前模型和上下文使用比例。长任务即使暂时没有新内容，也会定时刷新耗时，表示任务仍在运行。

## 三、在卡片内继续对话

每张回答卡片下方都有独立单行输入框，输入后按 Enter 直接提交。飞书仅为 form 外的独立输入组件提供 `input_value` 回调，按钮无法读取相邻独立输入框的值，因此新版取消发送/清空按钮。收到问题后先刷新本轮状态，再清空输入框；生成期间只流式显示新问题和新回答，完成后才把此前历史恢复到分页正文中，不会额外产生用户消息气泡。

卡片和任务是一对一绑定关系。即使当前任务已经切换，在旧卡片中提问仍会进入旧卡片自己的 Claude 上下文，不会误发到其他任务。卡片输入最多支持 1000 个字符；图片、文件、语音和视频仍需通过飞书聊天发送。

## 四、回答卡片按钮

根据任务状态，回答卡片会提供以下操作：

| 按钮 | 作用 |
| --- | --- |
| 所有任务 | 另外发送已有任务列表，不替换当前回答卡。 |
| 新任务 | 创建独立任务并另外发送一张空闲流式卡片；当前卡片保持不变。 |
| 停止生成 | 立即终止当前一轮处理，但保留任务和已有上下文。 |
| 重新生成 | 重新读取原消息的最新内容并再次提问；编辑原消息后可用此按钮。 |
| 继续 | 让 Claude 接着当前答案继续处理。 |
| 解释一下 | 要求 Claude 用更容易理解的方式解释刚才的回答。 |
| 生成文档 | 让 Claude 把当前结果整理成 Markdown 文件。 |
| 打开任务 | 在当前 Mac 上打开该任务的本地工作目录。 |
| 查看日志 | 在当前 Mac 上打开桥接日志目录。 |
| 生成文件 | 在当前 Mac 上打开该任务的输出目录。 |

飞书 Card 2.0 不提供卡片内部滚动容器，因此正文采用固定窗口翻页：每页最多 4 轮、约 4200 字符，在“更多”菜单点击“较早对话”或“回到最新”即可在同一卡片回看。历史持久化到本机，服务重启后仍可恢复；分页只影响展示，Claude 上下文不会丢失。

操作栏采用单行紧凑布局，直接显示“所有任务”和“停止”，新任务及其余操作统一放进“更多”菜单。任务列表中的“打开”会为已有任务唤起新的活动流式卡片。多张不同任务卡片可以同时存在，并分别通过自己的输入框继续对话。

## 五、多任务管理

每个任务拥有独立的 Claude 会话和上下文，适合同时处理不同项目或主题。

常用命令：

```text
/help                         查看帮助
/new <任务名>                 新建并切换到一个任务
/tasks                        查看交互式任务列表
/use <任务ID>                 切换当前任务
/ask <任务ID> <问题>          向指定任务提问
/status                       查看当前任务状态
/stop <任务ID>                停止指定任务正在生成的内容
/rename <任务ID> <新名称>     重命名任务
/close <任务ID>               删除任务
```

发送 `/tasks` 后，可以直接在任务列表卡片中：

- 切换当前任务。
- 停止正在运行的任务。
- 输入新名称并重命名任务。
- 删除不再需要的任务。

任务状态会保存到本机。电脑重启后任务列表仍然存在，再次向任务提问时会尝试恢复原来的 Claude 会话上下文。

## 六、图片、文件、语音和视频

机器人支持接收以下飞书消息：

- 文本和富文本。
- 图片。
- 普通文件。
- 语音消息。
- 视频或媒体文件。

收到媒体消息后，桥接会先把资源下载到本机运行目录，再把本地文件路径交给 Claude Code 分析。分析效果取决于文件格式以及当前 Claude Code 可使用的工具。

你可以在发送文件后补充具体要求，例如：

```text
总结这份文件的核心结论，并列出需要我处理的事项
```

## 七、引用回复

回复某一条飞书消息并提问时，机器人会读取被引用消息，并明确把它作为当前问题的上下文交给 Claude。

适合以下场景：

- 针对 Claude 上一条回答继续追问。
- 引用一段较早的需求，要求补充或修改。
- 引用文件消息，说明需要怎样分析。

## 八、编辑消息后重新提问

如果原问题写错了，可以先编辑飞书中的原消息，再点击回答卡片中的“重新生成”。系统会重新读取编辑后的消息，而不是使用旧内容。

飞书当前不会把消息编辑事件直接作为一次新问题推送给桥接，因此编辑完成后仍需手动点击“重新生成”。

## 九、代码、日志和表格展示

回答内容会针对飞书卡片进行整理：

- 代码使用 Markdown 代码块展示，并尽量补充语言标记。
- 日志使用等宽代码块，减少格式错乱。
- Markdown 表格保留行列结构，由飞书卡片渲染。
- 过多的空行会自动压缩。
- 特别长的连续对话会优先隐藏最旧轮次，保留最新进展。

## 十、失败与重试

任务失败时，卡片会显示便于理解的原因，例如服务暂时繁忙、权限不足或等待超时，不会直接把底层异常堆栈展示给用户。

可以点击“重新生成”重试。如果持续失败，可点击“查看日志”在当前 Mac 上查看详细记录。

## 十一、本地文件位置

后台运行数据默认保存在：

```text
~/Library/Application Support/lark-claude-bridge/
```

常见目录和文件：

| 位置 | 内容 |
| --- | --- |
| `logs/bridge.log` | 桥接服务运行日志。 |
| `logs/tasks-state.json` | 任务列表和会话状态。 |
| `logs/cards-state.json` | 任务与流式卡片实体、消息和更新序号的对应关系。 |
| `attachments/<message_id>/` | 从飞书下载的图片、文件、语音和视频。 |
| `claude-workspace/<任务ID>/` | 每个任务独立的本地工作目录。 |
| `claude-workspace/<任务ID>/output/` | “生成文档”等操作产生的文件。 |

“打开任务”“查看日志”和“生成文件”按钮只会在运行桥接服务的当前 Mac 上打开 Finder，不会把本机目录远程发送到飞书客户端。

## 十二、使用建议

- 不同项目使用不同任务，避免上下文相互干扰。
- 长任务先说明目标、范围和期望输出格式。
- 分析附件时，明确指出需要总结、提取、对比还是修改。
- 上下文使用比例较高时，可以新建任务，或让 Claude 先生成阶段总结再继续。
- 涉及删除文件、发布上线或对外发送内容时，在问题中明确要求 Claude 先确认再执行。

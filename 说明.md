# lark-ai-bridge

把飞书 IM 文本消息转给本机交互式 `claude`，再把 Claude 的回答回复到原消息。

这个脚本使用 Claude Code 的 `--print --output-format stream-json --include-partial-messages` 接口。它直接消费实时 NDJSON 事件，把思考增量、工具调用状态和正文 token 持续更新到飞书流式卡片，同时用 `session_id` 保持多轮对话上下文。

## 前置条件

- 本机可运行 `lark-cli`
- 本机可运行 `claude`
- 已运行 `python3 -m pip install -r requirements.txt` 安装官方飞书 Python SDK
- 飞书应用已订阅 `im.message.receive_v1`
- bot 有接收/发送消息所需权限，并已加入要测试的会话
- lark-cli 已配置 Claude 桥接机器人专用 profile

先复制并编辑本机配置，`LARK_PROFILE` 建议使用独立名称，避免误连到其他业务机器人：

```bash
cp .env.example .env
```

再检查本机 profile：

```bash
lark-cli profile list
```

如果没有这个 profile，用该机器人的 App Secret 添加：

```bash
read -rsp 'App Secret: ' LARK_APP_SECRET
printf '\n'
printf '%s' "$LARK_APP_SECRET" | lark-cli profile add \
  --name "$LARK_PROFILE" \
  --app-id "$LARK_APP_ID" \
  --app-secret-stdin \
  --brand feishu
unset LARK_APP_SECRET
```

App Secret 会由 `lark-cli` 写入系统 Keychain，不需要填入 `.env`。真实 `.env` 已被 Git 忽略，仓库只保留 `.env.example`。

## 启动

```bash
cd /path/to/lark-ai-bridge
./run.sh
```

联调时只处理一条消息后退出：

```bash
./run.sh --once
```

只看流程、不真正发飞书回复：

```bash
./run.sh --dry-run --once
```

## 飞书内命令

机器人支持多个独立 Claude 任务窗口。每个任务第一次收到问题时才会启动自己的交互式 Claude 会话。

```text
/help 查看帮助
/new <任务名> 新开一个独立 Claude 任务窗口
/tasks 查看所有任务和进度
/use <任务ID> 切换当前对话任务
/ask <任务ID> <问题> 指定某个任务继续问
/status 查看当前任务状态
/close <任务ID> 关闭一个任务窗口
/stop <任务ID> 停止正在生成的任务
/rename <任务ID> <新名称> 重命名任务
```

每条普通飞书消息都会创建一个新任务和一张新流式卡片。需要延续某个任务时，在对应卡片的输入框中继续提问；斜杠命令不会自动创建任务。

`/tasks` 或每张回答卡的“所有任务”按钮会返回交互式任务列表卡片。点击任务的“打开”会另外唤起该已有任务的新活动流式卡片，不替换任务列表；也可停止、通过输入框重命名或删除任务。

## 回答卡片

每个任务复用一张回答卡片，避免连续对话产生大量重复卡片。卡片实时显示：

- 当前阶段：思考中、搜索中、读取文件、执行命令、整理答案。
- 本轮耗时、当前模型、上下文使用比例。
- Claude 的实时思考、工具状态和正文 token。
- 长任务每 10 秒刷新一次耗时，即使暂时没有新 token 也能看出任务仍在运行。

卡片正文按时间顺序分页显示“你 / Claude”问答，每页最多 4 轮、约 4200 字符，通过“更多”里的“较早对话 / 回到最新”在同一卡片切换。飞书 Card 2.0 没有内部滚动容器，分页用于稳定控制卡片高度；历史会持久化，服务重启或重新打开任务卡后仍能恢复，Claude 会话上下文不受分页影响。

每张回答卡片都有自己的独立单行输入框，输入后按 Enter 发送。飞书仅对 form 外的独立输入组件返回 `input_value`，按钮无法读取相邻输入组件，因此新版移除发送/清空按钮。收到回调后先刷新本轮状态，再通过下一 CardKit 序号清空输入组件；该辅助请求最多等待 5 秒，超时不会阻塞 Claude。生成期间正文只包含当前问答，定稿时才恢复分页历史；输入最多 1000 个字符，附件仍需通过飞书聊天发送。

卡片操作栏显示“所有任务”“停止”和“更多”。“所有任务”会另外发送任务列表；“新任务”“继续”、重新生成、解释一下、生成文档、打开本地任务、查看日志和查看生成文件收入“更多”菜单。任务卡保持流式模式，卡片内后续提问可继续更新同一活动卡。停止会终止本轮 Claude 子进程但保留任务。

按钮依赖开放平台“事件与回调 > 回调配置 > 卡片回传交互（`card.action.trigger`）”以及 `im:message:readonly` 权限。

消息事件与卡片回调由 `lark_bridge/event_gateway.py` 在同一条官方 SDK WebSocket 长连接中注册。回调到达时网关会先立即向飞书确认，再把事件通过 NDJSON 交给桥接主进程的后台线程处理，避免业务执行时间导致按钮超时或出现 `200671`。

## 图片、文件、语音与引用

桥接会读取飞书消息详情，从图片、文件、语音和视频消息中提取资源键，并下载到：

```text
~/Library/Application Support/lark-ai-bridge/attachments/<message_id>/
```

下载后的绝对路径会连同用户正文交给 Claude Code。引用回复会额外读取被引用消息正文，以“用户引用的消息”上下文传给 Claude。

## 回复行为

普通消息和 `/ask` 这类需要等待 Claude 的问题，默认走**流式卡片**：脚本先用 cardkit 接口创建一张开启 `streaming_mode` 的卡片实体，初始显示“AI思考中...”，然后把 Claude 的思考增量、工具调用状态和累计正文按节流间隔（默认 0.8 秒）更新进卡片；Claude 结束本回合后关闭流式模式给卡片定稿。上游服务发生临时错误并自动重试时，卡片也会显示当前重试次数。

流式卡片依赖机器人应用开通 cardkit 权限（如 `cardkit:card`）。如果建卡或发卡失败，脚本会自动回退到旧的文本占位路径：先回复 `AI思考中...`，Claude 完成后用飞书“编辑消息”接口把占位消息替换成真实回答，编辑失败再补发一条最终回答。`--dry-run` 模式也始终走文本路径，方便测试。

无论走哪条路径，桥接都会等 Claude 当前回合真正结束后再定稿，不会把“先找一下相关代码”这类工具调用前的阶段性文本当成最终回复（流式路径下这些中间文本只是临时显示，最终仍以 end_turn 文本为准）。长任务会放到后台线程里处理，因此你可以继续在飞书里发 `/tasks` 或 `/status` 查看任务是否还在 `思考中`。

`--claude-timeout` 现在是软提示阈值，不再是硬失败时间。默认 180 秒后脚本只会在本地日志记录“仍在执行”，并继续等待 Claude 的最终回答。

这个替换能力需要机器人能编辑自己发送的消息。飞书开放平台里建议给机器人开通 `im:message:update`，或者确保已有 `im:message` / `im:message:send_as_bot` 覆盖到编辑消息接口。

Claude 工作区根目录会放置通用的 `CLAUDE.md`，说明这些问题来自飞书并限制默认访问边界。你可以在本机运行目录中补充自己的项目和知识库映射；后续安装更新不会覆盖已有文件，也不要把包含个人路径的版本提交到公开仓库。

## 任务持久化

任务元数据（任务列表、标题、状态、轮次、最近问答、每个任务底层的 Claude `session_id`、每个发送者当前选中的任务、下一个任务编号）会实时落盘到 `logs/tasks-state.json`。任务与 CardKit 实体、卡片消息和更新序号的对应关系会保存到 `logs/cards-state.json`，服务重启后旧任务卡片仍能继续接收卡片内提问。落盘采用「先写临时文件再原子 rename」，保证读到的永远是完整 JSON。

因此关机或重启桥接后任务不会丢：脚本启动时自动从 `tasks-state.json` 恢复任务列表，恢复出来的任务状态标记为 `待恢复`。当你下一次向某个恢复的任务发消息时，脚本会用 `claude --resume <session_id>` 把原来的 Claude 会话拉起来，续接完整对话上下文，而不是新开一个空会话。

若 `tasks-state.json` 缺失或损坏，脚本会安全地从空状态启动，不会因此启动失败。

## 日志

- 桥接日志：`logs/bridge.log`
- 任务持久化状态：`logs/tasks-state.json`
- 任务卡片持久化状态：`logs/cards-state.json`
- 飞书事件 stderr：`logs/lark-event.stderr.log`
- Claude 正式会话日志：`~/.claude/projects/.../<session-id>.jsonl`
- 每个任务的本地工作目录：`claude-workspace/t1`、`claude-workspace/t2` 等
- 每个任务的 Claude TUI 日志：`logs/t1/claude-pty.log`、`logs/t2/claude-pty.log` 等

## 常见问题

如果启动后收不到消息，先运行：

```bash
lark-cli --profile "$LARK_PROFILE" event consume im.message.receive_v1 --max-events 1 --timeout 30s --as bot
```

如果回复失败，通常是 bot 没有 `im:message:send_as_bot` 权限，或 bot 没在目标群/会话里。

# macOS 开机自启动配置说明

## 实现方式

服务使用当前用户的 macOS `LaunchAgent`：

- 开机并登录当前用户后自动启动；未登录用户时不会运行。
- 进程异常退出后由 `launchd` 自动拉起。
- LaunchAgent 标识为 `com.imber.visual-lark-bridge`。
- 配置文件位于 `~/Library/LaunchAgents/com.imber.visual-lark-bridge.plist`。

macOS 可能限制无界面后台进程访问 Desktop 等目录。为避免授予 Python“完整磁盘访问权限”，安装脚本会把运行副本放到：

```text
~/Library/Application Support/visual-lark-bridge
```

LaunchAgent 只访问这个运行目录，因此不需要修改系统隐私授权。

## 安装或更新

在项目目录运行：

```bash
cd /path/to/visual-lark-bridge
cp .env.example .env
# 编辑 .env，填写自己的 LARK_PROFILE 和 LARK_APP_ID。
./install-launch-agent.sh
```

安装脚本会完成以下操作：

1. 检查本机 `.env` 以及 `python3`、`claude` 和 `lark-cli`。
2. 首次安装时迁移现有任务状态、日志和 Claude 工作区。
3. 把最新桥接脚本和权限为 `0600` 的 `.env` 同步到非受保护的运行目录。
4. 根据本机实际命令路径生成不含敏感值的 LaunchAgent plist。
5. 通过 `run.sh` 加载 `.env`，重新启动服务。

以后修改了 `visual_lark_bridge.py`，再次执行安装脚本即可部署新版本。运行目录中的任务状态和日志会保留。

## 当前验证结果

安装完成后，服务应显示：

```text
state = running
last exit code = (never exited)
```

桥接日志应出现：

```text
启动飞书事件监听
桥接已就绪，等待飞书消息
```

服务同时监听 `im.message.receive_v1` 和 `card.action.trigger`，分别处理消息与卡片按钮。运行目录迁移后，如果持久化的旧 Claude 会话 ID 无法在新工作目录恢复，桥接会自动创建新会话、重试当前消息并保存新的会话 ID。

## 常用管理命令

查看状态：

```bash
launchctl print gui/$(id -u)/com.imber.visual-lark-bridge
```

查看桥接日志：

```bash
tail -f "$HOME/Library/Application Support/visual-lark-bridge/logs/bridge.log"
```

查看 launchd 错误日志：

```bash
tail -f "$HOME/Library/Application Support/visual-lark-bridge/logs/launchd.err.log"
```

重新安装并重启：

```bash
cd /path/to/visual-lark-bridge
./install-launch-agent.sh
```

停止并取消自启动：

```bash
launchctl bootout gui/$(id -u)/com.imber.visual-lark-bridge
rm "$HOME/Library/LaunchAgents/com.imber.visual-lark-bridge.plist"
```

取消后如需恢复，重新运行 `./install-launch-agent.sh`。

## 故障排查

### 服务没有运行

先重新执行安装脚本，再检查：

```bash
launchctl print gui/$(id -u)/com.imber.visual-lark-bridge
cat "$HOME/Library/Application Support/visual-lark-bridge/logs/launchd.err.log"
```

安装脚本会对 `launchd` 卸载后的短暂加载竞态自动重试。如果连续重试仍失败，应保留命令输出继续排查，不要改回直接从 Desktop 启动的旧 plist。

### 飞书能收到消息但 Claude 不回答

检查桥接日志和对应任务的 PTY 日志：

```bash
tail -100 "$HOME/Library/Application Support/visual-lark-bridge/logs/bridge.log"
find "$HOME/Library/Application Support/visual-lark-bridge/logs" \
  -name claude-pty.log -type f -exec tail -50 {} \;
```

### 源码更新后行为没有变化

LaunchAgent 运行的是 `Application Support` 中的副本，不会直接执行 Desktop 里的源码。修改源码后必须重新运行：

```bash
./install-launch-agent.sh
```

# 代码规范

- 所有交流、注释和文档使用中文。
- 先通过代码、日志、测试或复现定位根因，再做最小改动；不重构无关区域。
- 只有存在实际复用时才抽离函数；命令、事件类型、阈值和状态使用具名常量。
- 访问事件字段、接口返回值、文件和子进程结果前处理空值与失败。
- 函数、方法和关键参数说明用途；业务变量说明存储内容；复杂兼容和 workaround 注释 WHY。
- 后台线程、子进程、stdin 与定时刷新必须有明确停止、超时和资源清理路径。
- 用户可见错误提供可操作信息，日志保留上下文但不得泄露凭据。

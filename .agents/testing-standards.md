# 测试规范

- 测试只覆盖本次新增或修改行为；修复 Bug 时先增加可复现用例。
- 使用 unittest、mock、临时目录和假会话隔离飞书、Claude CLI、文件系统和子进程。
- 线程测试使用 Event 等确定性同步，不使用任意 sleep；用例结束必须释放阻塞线程和子进程。
- 每个用例重置类级事件、环境变量和 mock，不能依赖执行顺序。
- 解析测试覆盖未知字段、缺失字段、重复事件、分页边界和流式异常结束。
- 不连接真实飞书、不读取真实 `.env`、Keychain、附件或 Claude 会话。

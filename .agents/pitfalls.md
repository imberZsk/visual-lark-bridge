# 踩坑记录

- 双 `lark-cli`/双消费者会导致卡片 handler 漏注册并出现 `200671`，必须保留单 WebSocket 网关。
- 飞书独立输入组件按 Enter 才能可靠返回 `input_value`；放入 form 后 Enter 不提交，不能恢复无效按钮方案。
- 收到卡片输入后先显示本轮问题与思考状态，再清空输入；完成前不要把历史分页重新混入流式区域。
- “重新生成”需要重新读取原消息，以支持用户编辑后的最新内容。
- 媒体下载到运行目录 `attachments/`，不得写入仓库或提交；失败时只影响当前媒体消息。
- 长任务每 10 秒刷新耗时，同一任务更新同一卡；刷新线程与生成完成必须避免互相覆盖。
